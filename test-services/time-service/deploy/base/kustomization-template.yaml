apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - service-account.yaml
  - deployment.yaml
  - ingress.yaml
  - service.yaml
  - canary.yaml

images:
  - name: time-service
    newTag: ${APP_VERSION}

commonLabels:
  app: time-service
